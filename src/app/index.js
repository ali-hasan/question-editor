import React from "react";
import ReactDOM from "react-dom";

//Custom imports
import Layout from "./Layout"

const app = document.getElementById("app"); //app element
ReactDOM.render(<Layout />, app); //render app
